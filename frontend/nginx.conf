server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router needs this - any path that isn't a real file falls
    # back to index.html so client-side routing still works on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
